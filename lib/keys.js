#!/usr/bin/env node
/**
 * Spark Server Library - Keys
 * Generate keypairs for app use, and verify client key / MAC or client key / IP
 * pairs.
 * Critical library, used by all application functions.
 */

/**
 * Return a public key.
 * @return String   The public key currently used by the application,
 *                  in JSON format.
 */
function getPublic(){

}

/**
 * Signs a given key using the server's private key.
 * @param String    key         The key to sign for verification.
 * @return String   The signed key.
 */
function sign( key ){

}
